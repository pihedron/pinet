name: pinet
description: A server app using the shelf package and Docker.
version: 0.0.1
repository: https://github.com/pihedron/pinet

environment:
  sdk: ^3.3.1

dependencies:
  args: ^2.4.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  timeago: ^3.6.1

dev_dependencies:
  http: ^1.1.0
  lints: ^3.0.0
  test: ^1.24.0
